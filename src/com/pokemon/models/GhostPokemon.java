package com.pokemon.models;
import com.pokemon.enums.PokemonType;
import java.util.List;

public class GhostPokemon extends Pokemon { public GhostPokemon(String n, List<Move> m) { super(n, PokemonType.GHOST, m); } }
